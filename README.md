# Instructions
Preferably create a virtual enviroment:

```shell
python3 -m venv .venv
```

Acivate virtual environment:
```shell
. .venv/bin/activate
```
Install packages:
```shell
pip install -r requirements.txt 
```

Run the script:
```shell
python3 scraper.py
```